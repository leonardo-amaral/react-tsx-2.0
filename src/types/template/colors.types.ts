export type TColors = {
  primary: {
    id: string
    name: string
    color: string
    updatedAt: string
  }
  secondary: {
    id: string
    name: string
    color: string
    updatedAt: string
  }
  tertiary: {
    id: string
    name: string
    color: string
    updatedAt: string
  }
}
